{
  "name": "offline-arcade",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.4.0",
  "scripts": {
    "dev": "pnpm --filter @apps/arcade dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "preview": "pnpm --filter @apps/arcade preview"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
